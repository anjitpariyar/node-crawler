{
  "name": "nodejs-crawller",
  "version": "1.0.0",
  "description": "it crawl given sites and grab product details",
  "main": "src/index.js",
  "scripts": {
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_OPTIONS='--inspect' ts-node src",
    "dest": "cross-env NODE_OPTIONS='--inspect' ts-node src/destination",
    "build": "rimraf dist && tsc ",
    "check": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mongoose": "^7.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/mongoose": "^5.11.97",
    "@types/node": "^18.16.3",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.15",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.5.5"
  }
}
