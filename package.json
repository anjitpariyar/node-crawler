{
  "name": "nodejs-crawller",
  "version": "1.0.0",
  "description": "it crawl given sites and grab product details",
  "main": "src/index.js",
  "scripts": {
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_OPTIONS='--inspect' ts-node src",
    "build": "rimraf dist && tsc ",
    "check": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.15",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.5.5"
  }
}
